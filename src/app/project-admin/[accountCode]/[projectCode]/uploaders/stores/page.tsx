import { PaPlaceholder } from "@/components/project-admin/pa-placeholder";

export default function StoresMasterPage() {
  return (
    <PaPlaceholder
      eyebrow="Uploaders"
      title="Stores Master"
      description="Manage store codes, status, and store-level UDF fields for this project."
    />
  );
}
