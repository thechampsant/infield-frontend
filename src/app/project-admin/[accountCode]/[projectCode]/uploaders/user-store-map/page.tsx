import { PaPlaceholder } from "@/components/project-admin/pa-placeholder";

export default function UserStoreMapPage() {
  return (
    <PaPlaceholder
      eyebrow="Uploaders"
      title="User-Store Map"
      description="Configure direct and PJP mapping between users and stores."
    />
  );
}
