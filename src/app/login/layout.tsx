import type { ReactNode } from "react";
import "@/styles/infield-auth.css";
import "@/styles/infield-brand.css";

export default function LoginLayout({ children }: { children: ReactNode }) {
  return children;
}
